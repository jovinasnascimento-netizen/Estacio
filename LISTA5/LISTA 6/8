#include <stdio.h>

int main() {
int a [5], b[5],c[5];
int pos;

for(pos = 0; pos < 5; pos++) {
    printf("Digite o %d° valor do vetor a:",pos +1);
    scanf("%d",&a[pos]);
}
for(pos = 0; pos < 5 ; pos++){
  printf("Digite o %d° valor do vetor b:",pos +1);
  scanf("%d",&b[pos]);
}
 for(pos = 0; pos < 5 ; pos++){
  printf("Digite o %d° valor do vetor c:",pos +1);
  scanf("%d",&c[pos]);
}
for(pos 0; pos < 5; pos++) { printf("Digite o de valor do vetor b: ", pos+1); scanf("%d", &b[pos]);

for(pos 0; pos < 5; pos++) { printf("Digite o de valor do vetor b: ", pos+1); scanf("%d", &b[pos]);

}

return 0;
}