#include <stdio.h>
  int main(){
  int numeros[2];
  int i;
  int verificacao;
  for(i=0 ;i < 2; i++){
      printf("Digite %d° numeros",i+1);
      scanf("%d", &numeros[i]);
  }
  for(int pos=0; pos<2;pos++){
  printf("%d° numero[%d]: %d\n", pos+1,numeros[pos]);
  printf("+++++++++++++++++++++++++++++++++++++++++++++++\n");
  }
  
  return 0;
  }