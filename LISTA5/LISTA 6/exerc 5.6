#include <stdio.h>
 
 int main(){
 float numeros [6];
 int pos;
 int media, soma =0;
 
 for(pos=0; pos< 6 ; pos++){
printf("Digite o%d°numero:",pos+1);
 scanf("%f",&numeros[pos]);
 soma+=numeros[pos];
 }
 media=soma/6;
 printf("A media é igual:%f ",soma);
 
 return 0;
 }
 