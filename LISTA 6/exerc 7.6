#include <stdio.h>

int main() {
int numeros[7];
int pos;

for(pos = 0; pos < 7; pos++) {
    printf("Digite o %d° valor:",pos +1);
    scanf("%d",&numeros[pos]);
}
for(pos = 7; pos >=0; pos--){
    printf("%d° numero %d\n",pos+1, numeros[pos]);
}
 
return 0;
}